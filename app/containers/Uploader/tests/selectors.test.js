// import { fromJS } from 'immutable';
// import { makeSelectUploaderDomain } from '../selectors';

// const selector = makeSelectUploaderDomain();

describe('makeSelectUploaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
