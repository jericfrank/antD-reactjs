// import { fromJS } from 'immutable';
// import { makeSelectVideosDomain } from '../selectors';

// const selector = makeSelectVideosDomain();

describe('makeSelectVideosDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
